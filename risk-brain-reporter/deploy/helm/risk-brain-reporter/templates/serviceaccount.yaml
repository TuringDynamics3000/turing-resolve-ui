# Risk Brain Reporter — Kubernetes Service Account (IRSA)
#
# This service account is bound to the IAM role via IRSA.
#
# Author: TuringCore National Infrastructure Team
# Version: 1.0

apiVersion: v1
kind: ServiceAccount
metadata:
  name: risk-brain-reporter-sa
  namespace: risk-brain-reporter
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/risk-brain-reporter-role-prod
