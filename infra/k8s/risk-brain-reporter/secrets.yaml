# Risk Brain Reporter — Kubernetes Secrets
#
# This secret contains:
# - Prometheus URL
# - API token (for on-demand regulator annex generation)
#
# NO DB credentials, NO ledger credentials, NO Kafka credentials, NO core platform secrets.
#
# Author: TuringCore National Infrastructure Team
# Version: 1.0

apiVersion: v1
kind: Secret
metadata:
  name: reporter-secrets
  namespace: risk-brain-reporter
type: Opaque
stringData:
  prom_url: https://amp-query.internal
  api_token: CHANGE_ME_IN_PRODUCTION
